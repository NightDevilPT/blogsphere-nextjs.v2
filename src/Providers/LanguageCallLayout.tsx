
import { ChildrenNode } from "@/types/type";
import React from "react";

const LanguageCallLayout = ({ children }: ChildrenNode) => {
	return <React.Fragment>{children}</React.Fragment>;
};

export default LanguageCallLayout;
